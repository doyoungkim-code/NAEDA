services:
  # ============================================================
  # PostgreSQL DB
  # ============================================================
  postgres:
    build:
      context: .                        # infra/ 기준
      dockerfile: postgres/Dockerfile   # SQL을 이미지 안에 내장
    container_name: naeda-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      TZ: Asia/Seoul
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data  # 데이터만 볼륨으로 영속화
    networks:
      - naeda-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================================
  # Redis
  # ============================================================
  redis:
    image: redis:7.2-alpine
    container_name: naeda-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes
    environment:
      TZ: Asia/Seoul
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - redis_data:/data
    networks:
      - naeda-network
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================================
  # AI 서버 (FastAPI)
  # ============================================================
  ai-server:
    build:
      context: ../AI
      dockerfile: Dockerfile
    container_name: naeda-ai
    restart: unless-stopped
    environment:
      TZ: Asia/Seoul
      AI_SERVICE_TOKEN: ${AI_SERVICE_TOKEN}
    ports:
      - "${AI_PORT}:8000"
    networks:
      - naeda-network
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

# ============================================================
# 볼륨
# ============================================================
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

# ============================================================
# 네트워크
# ============================================================
networks:
  naeda-network:
    driver: bridge